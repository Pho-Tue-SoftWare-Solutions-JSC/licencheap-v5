# Changelog (EN)

## 1.1.0

- Auto-register and assign client widgets (details / change IP / docs) during upgrade.

## 1.0.0

- Initial module release: provision/renew/suspend/unsuspend/change IP.
- Command generation from `products.json` with optional remote `ShowHelp`.
